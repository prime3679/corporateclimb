// ─── TREASURE FLOORS (The Supply Closet) ────────────────────
// The fourth elevator, open on one scheduled floor per act. The fight
// is the standard one, but the payout is skimmed in kind: half the
// Stock Options, and a pick-1-of-3 supply cache after the win.

/** Victory payout multiplier while raiding the closet. */
export const TREASURE_PAYOUT_MULT = 0.5

/** How many supplies a cache offers (distinct items). */
export const TREASURE_LOOT_CHOICES = 3

/** Petty-cash consolation for leaving the cache (or full pockets). */
export const TREASURE_CONSOLATION_OPTIONS = 25
